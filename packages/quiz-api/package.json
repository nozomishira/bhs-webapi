{
  "name": "@bhs/quiz-api",
  "version": "0.1.0",
  "description": "bhs-indonesia-webapp Quiz API - Lambda handler",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:clean": "rimraf dist && tsc",
    "package": "npm run build:clean && cp -r node_modules dist/ && cd dist && zip -r ../function.zip . && cd .."
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@bhs/shared": "*",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^22.15.29",
    "@types/uuid": "^10.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
