{
  "name": "@bhs/shared",
  "version": "0.1.0",
  "description": "Shared types and utilities for bhs-webapi packages",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:clean": "rimraf dist && tsc"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "rimraf": "^6.0.1"
  }
}
