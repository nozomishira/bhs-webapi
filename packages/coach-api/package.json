{
  "name": "@bhs/coach-api",
  "version": "0.1.0",
  "description": "bhs-indonesia-webapp Coach API - Learning coach agent Lambda",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:clean": "rimraf dist && tsc",
    "package": "npm run build:clean && cp -r node_modules dist/ && cd dist && zip -r ../function.zip . && cd .."
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.700.0",
    "@bhs/shared": "*",
    "aws4": "^1.13.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/aws4": "^1.11.6",
    "@types/node": "^22.15.29",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
