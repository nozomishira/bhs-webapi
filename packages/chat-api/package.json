{
  "name": "@bhs/chat-api",
  "version": "0.1.0",
  "description": "bhs-indonesia-webapp Chat API - Bedrock 会話練習 Lambda",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:clean": "rimraf dist && tsc",
    "package": "npm run build:clean && cp -r node_modules dist/ && cd dist && zip -r ../function.zip . && cd .."
  },
  "dependencies": {
    "@aws-crypto/sha256-js": "^5.2.0",
    "@aws-sdk/client-bedrock-runtime": "^3.700.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/credential-provider-node": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@smithy/protocol-http": "^4.1.0",
    "@smithy/signature-v4": "^4.2.0",
    "@bhs/shared": "*",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^22.15.29",
    "@types/uuid": "^10.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
